# Tools

Non-production helper executables:

- `admin`: operator token and administration CLI.
- `perf-lab`: repeatable real-time media performance gates.

Tools may compose workspace capabilities but must not become runtime
dependencies of production libraries or services.
