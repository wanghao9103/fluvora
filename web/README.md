# Web integration

The browser implementation is packaged in `sdk/web`. It owns WebRTC offer/answer integration,
P2P signaling, ICE server refresh, weak-network sender adaptation, HLS fallback and the
`fluvora.room.v1` DataChannel envelope codec.
